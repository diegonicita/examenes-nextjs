import DisplayConsults from '@/app/(pages)/consults/displayConsults'

const Page = async () => {
  return <DisplayConsults />
}

export const metadata = {
  title: 'Admin Only',
}

export default Page
